const ENDPOINTS = {
  // ============================================
  // AUTHENTICATION
  // ============================================
  AUTH_LOGIN: "/api/auth/login",
} as const;

export default ENDPOINTS;
